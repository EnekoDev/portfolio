---
const { frontmatter } = Astro.props
---

<div class="card-body">
    <div class="card-title-place-timeline">
        <h3>{frontmatter.title}</h3>
        <h4>{frontmatter.place}</h4>
        <p>{frontmatter.timeline}</p>
    </div>
    <div class="card-description">
        <p>{frontmatter.description}</p>
    </div>
</div>

<style>

</style>